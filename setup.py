from distutils.core import setup
setup(
  name = 'crawlz',
  packages = ['crawlz'], # this must be the same as the name above
  version = "0.0.0",
  description = 'Crawlz allows the local cache contact information.',
  author = 'Jeremias Wong, Benjamin Soon',
  author_email = 'jellyjellyrobot@gmail.com',
  url = 'https://github.com/crawlzstage/crawlz', # use the URL to the github repo
  download_url = 'https://github.com/crawlzstage/crawlz/tarball/0.0.0', # I'll explain this in a second
  keywords = ['meh', 'failz', 'test'], # arbitrary keywords
  classifiers = [],
)